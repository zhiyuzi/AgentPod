[Unit]
Description=AgentPod Runtime
After=network.target

[Service]
Type=exec
User=agentpod
EnvironmentFile=/etc/agentpod/.env
ExecStart=/opt/agentpod/.venv/bin/agentpod serve --host 0.0.0.0 --port 8000
Restart=on-failure

[Install]
WantedBy=multi-user.target
